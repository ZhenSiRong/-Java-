package lesson;

import java.util.Scanner;

public class lessonthree {

	public static void main(String[] args) {
		int chang=0;
		int gao = 0;
		
		//接收三个值，三角形边长的值；
		//Tips:S = Math.sqrt(l*(l-a)*(l-b)*(l-c))
		Scanner inpScanner = new Scanner(System.in);
		int firstV = inpScanner.nextInt();
		int secondV = inpScanner.nextInt();
		int threeV = inpScanner.nextInt();
		if(secondV>threeV) {
			chang = Math.max(firstV, secondV);
			gao = 
		}
		int mianji = chang*gao;
			
		
		

	}

}
